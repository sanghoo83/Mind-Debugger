# Netlify 배포 설정 — private 저장소도 무료로 빌드/호스팅.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA 폴백: 실제 파일(sw.js·manifest·assets)은 그대로 서빙되고,
# 매칭 안 되는 경로만 index.html로 (PWA 라우팅 안전망).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
